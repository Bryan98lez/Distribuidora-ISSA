/* Estilos generales */
body {
    font-family: Arial, sans-serif;
    background-color: #f4f4f4;
    color: #333;
    margin: 0;
    padding: 0;
}

/* Encabezado */
header {
    background-color: #4CAF50; /* Color verde */
    color: white;
    padding: 15px;
    text-align: center;
}

/* Navegación */
nav {
    margin: 20px 0;
}

nav a {
    color: white;
    text-decoration: none;
    padding: 10px 15px;
    background-color: #333; /* Color de fondo para enlaces */
    border-radius: 5px;
}

nav a:hover {
    background-color: #555; /* Color de fondo al pasar el mouse */
}

/* Contenido principal */
main {
    padding: 20px;
}

/* Secciones */
section {
    margin-bottom: 20px;
    padding: 15px;
    background-color: white;
    border-radius: 5px;
    box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
}

/* Pie de página */
footer {
    text-align: center;
    padding: 10px;
    background-color: #4CAF50;
    color: white;
    position: relative;
    bottom: 0;
    width: 100%;
}

/* Estilos de botones */
button {
    background-color: #4CAF50; /* Color verde */
    color: white;
    border: none;
    padding: 10px 20px;
    border-radius: 5px;
    cursor: pointer;
}

button:hover {
    background-color: #45a049; /* Color al pasar el mouse */
}
